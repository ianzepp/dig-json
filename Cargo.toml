[package]
name = "dig-json"
version = "0.1.0"
edition = "2021"
description = "DNS lookup with JSON output"
license = "MIT"

[dependencies]
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
